import { Image } from "react-native";
import React from "react";
import { success } from "@constants/imports/icons";

const SuccessIcon = () => {
  return <Image source={success} style={{}} />;
};

export default SuccessIcon;
